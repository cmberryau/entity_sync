name: entity_sync
version: '0.0.1'
description: An entity sync library for Dart developers.
homepage: https://github.com/cmberryau/entity_sync

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  http: ^0.13.3
  drift: ^1.4.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  mockito: ^5.1.0
  build_runner: ^2.1.7
  moor_generator: ^4.6.0
  entity_sync_generator:
    git:
      url: https://github.com/lodlaw/entity_sync_generator
      ref: dependencies-upgrade
